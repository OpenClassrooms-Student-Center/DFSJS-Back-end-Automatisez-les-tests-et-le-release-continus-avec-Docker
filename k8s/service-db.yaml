apiVersion: v1
kind: Service
metadata:
  name: workshop-organizer-db-service
spec:
  selector:
    app: workshop-organizer-db
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  clusterIP: None
